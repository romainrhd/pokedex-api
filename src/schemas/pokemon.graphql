type Query {
    pokemons: [Pokemon]
    pokemon(nationalNumber: String!): Pokemon
}

type Mutation {
    createPokemon(nationalNumber: String!, name: String!): Pokemon
}

type Pokemon {
    nationalNumber: String
    name: String
    appearances: [Appearance]
}